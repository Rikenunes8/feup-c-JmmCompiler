import o1;
import OtherClass;

myClass {
    .field private a.i32;

    .method public check(b.i32).V {
        t1.i32 :=.i32 getfield(this,a.i32).i32;
        t1.i32 :=.i32 getfield(o1,a.i32).i32;

        putfield(this,a.i32,3.i32).V;
        putfield(o1,a.i32,3.i32).V;

        t1.i32 :=.i32 getstatic(OtherClass,a.i32).i32;

        putstatic(OtherClass,a.i32,b.i32).V;
    }
}
