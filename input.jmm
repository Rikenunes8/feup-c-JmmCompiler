import io;
class FirstCompiler {
	int a;
	public static void main(String[] args) {
        int b;
        int c;
        // FirstCompiler t;

        // t = new FirstCompiler();
        b = 2 + 3;
        c = b * 5;
        if (true && false && true)
            c = 3 + 4 * 2;
        else
            c = 6 - 3;

        io.print(b);
	}

	public int f(int d, boolean g) {
	    a = 1;
	    io.print(a);
	    return a;
	}

}
